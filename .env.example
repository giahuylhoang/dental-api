# Database (required for PostgreSQL; omit for SQLite)
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# API
PORT=8000

# Working hours for available slots (9am–5pm by default)
WORKING_HOUR_START=9
WORKING_HOUR_END=17
